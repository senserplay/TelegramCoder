START_PROMPT = """
СОЗДАЙ 4 УНИКАЛЬНЫХ ТЕМЫ ДЛЯ ПРОГРАММЫ В ОДНОМ ФАЙЛЕ. ОТВЕЧАЙ СТРОГО В ФОРМАТЕ: ["тема 1","тема 2","тема 3","тема 4"]

ПРАВИЛА ОТВЕТА:
1. ФОРМАТ: ТОЛЬКО МАССИВ JSON В ОДНУ СТРОКУ БЕЗ ПЕРЕНОСОВ
2. ЗАПРЕЩЕНО: Любой текст до/после массива, Markdown (```json, ```), комментарии
3. ЭКРАНИРОВАНИЕ: Все кавычки внутри тем ДОЛЖНЫ быть экранированы: \"
4. ДЛИНА: КАЖДАЯ тема ≤ 95 символов (считай пробелы!)

ПРАВИЛА СОДЕРЖАНИЯ ТЕМ:
- Тема должна быть конкретной и реализуемой в одном файле
- ЗАПРЕЩЕНО: "простая программа", "полезный скрипт", общие фразы
- ЗАПРЕЩЕНО: Темы, требующие внешних API или сложных зависимостей
- Разрешено: Локальные программы без интернета, консольные утилиты, алгоритмы

ПРИМЕРЫ:
✅ Хороший ответ: ["Генератор паролей с настройкой сложности","Конвертер валют без интернета","Таймер помидора для работы","Сортировка файлов по расширению"]
❌ Плохой ответ: ```json\n["тема"]\n```
❌ Плохой ответ: {"themes": ["тема"]}
❌ Плохой ответ: Вот темы: ["тема"]

ВАЖНО: Твой ответ должен быть пригоден для немедленного json.loads() без ошибок!
"""

BASIC_PROMPT = """
🔍 ПРОДОЛЖИ КОД ПО КОНТЕКСТУ (Python)

Контекст:
- Последние строки кода (массив строк, может быть пустым):
{last_code_lines}

Твоя задача: Предложить РОВНО 4 варианта СЛЕДУЮЩЕЙ строки кода (Python).

АЛГОРИТМ (СТРОГО):
1) Проанализируй {last_code_lines} как непрерывный фрагмент Python-кода.
2) Определи все доступные идентификаторы (имена переменных, функций, классов, модулей).
3) Любая предлагаемая строка ДОЛЖНА использовать только существующие идентификаторы.
4) Если логически требуется переменная/модуль, которого нет — сначала предложи корректную ИНИЦИАЛИЗАЦИЮ или ИМПОРТ.
5) Нельзя использовать имя до объявления.
6) Отступ новой строки должен совпадать с уровнем последнего блока (если есть символ ':').
7) Все строки должны быть простыми, однострочными, без вложенных выражений.

ЖЕСТКИЕ ПРАВИЛА (ФОРМАТ):
1. Ответ — ТОЛЬКО ОДНА СТРОКА: JSON-массив из 4 элементов. Пример: ["a","b","c","d"]
2. Все кавычки внутри строк ДОЛЖНЫ быть экранированы: \"
3. Никакого Markdown, комментариев и текста вне массива.
4. Каждая строка ≤ 95 символов.
5. Отступы — 4 пробела (PEP 8).
6. Только валидный синтаксис Python.

ЖЕСТКИЕ ПРАВИЛА (СОДЕРЖАНИЕ):
7. НЕЛЬЗЯ генерировать пустую строку или строку, состоящую только из пробелов.
8. НЕЛЬЗЯ повторять импорты, объявления переменных или функции, уже присутствующие в {last_code_lines}.
9. НЕЛЬЗЯ использовать переменные, функции или модули, которых нет в {last_code_lines}.
10. Строки вида просто "result" или "variable" БЕЗ операции, присваивания, вызова, условия и т.п. — ЗАПРЕЩЕНЫ.
11. Каждая из 4 строк ДОЛЖНА быть синтаксически корректной и НЕ должна приводить к ошибкам NameError, SyntaxError, UnboundLocalError.
12. Если невозможно продолжить логику — предложи безопасную нейтральную строку (например: flag = True).

ДОПОЛНИТЕЛЬНЫЕ ПРИМЕРЫ РАЗРЕШЁННЫХ СТРОК:
"for number in range(1, 11):"
"processed_items.add(item)"
"count = 0"

ПРИМЕРЫ ЗАПРЕЩЁННЫХ ВЫХОДОВ:
- Любой текст до/после массива.
- Ответ типа {{"options": [...]}}.
- Код без действий (например: "result").

ВАЖНО:
- Перед отправкой проверь: каждая из 4 строк должна быть валидной, непустой и безопасной.
"""


COMPLETE_PROMPT = """
🔧 ЗАВЕРШИ И ИСПРАВЬ КОД БЕЗ ИЗМЕНЕНИЯ ПОРЯДКА И ЛОГИКИ

Контекст:
- Текущий код (могут быть ошибки):
{last_code_lines}

Твоя задача: Исправить только синтаксические ошибки и сделать код корректным с точки зрения Python.
- НЕ удаляй строки
- НЕ меняй порядок строк
- НЕ добавляй новый функционал или вызовы
- НЕ меняй существующую логику
- Код может падать при запуске, если это не синтаксическая ошибка

ЖЕСТКИЕ ПРАВИЛА:
1. Исправляй только:
   - скобки и двоеточия
   - отступы (4 пробела на каждый уровень)
   - опечатки в именах переменных/функций
   - недостающие закрывающие символы
   - недостающие импорты, если явно нужны
2. НЕ перемещай строки
3. НЕ удаляй существующие строки
4. НЕ добавляй новый функционал
5. Максимальная длина строки ≤ 95 символов
6. Строка не может быть пустой или состоять только из пробелов
7. Комментарии должны быть на том же языке, что и исходный код
8. Каждая строка ДОЛЖНА быть синтаксически корректной с точки зрения Python
9. Любые строки вида просто "variable" или "result" без операции/присваивания — ЗАПРЕЩЕНЫ
10. Все отступы должны быть корректно расставлены (4 пробела для каждого уровня блока)

ФОРМАТ ВЫХОДА:
- JSON-массив в одну строку: ["строка1","строка2","строка3","строка4"]
- Все кавычки внутри строк экранируются: \"
- Никаких Markdown, комментариев или текста вне массива

ДОПОЛНИТЕЛЬНЫЕ ПРИМЕРЫ РАЗРЕШЁННЫХ СТРОК:
"if x > 5:"  # исправлено двоеточие
"result = []"  # исправлена инициализация
"print(x)"  # исправлен вызов функции

ПРИМЕРЫ ЗАПРЕЩЁННЫХ ВЫХОДОВ:
- Любой текст до/после массива
- Перемещение или удаление существующих строк
- Добавление нового функционала

ВАЖНО:
- Перед отправкой проверь: каждая из 4 строк должна быть валидной, непустой, с правильными отступами и не нарушать исходный порядок.
"""
