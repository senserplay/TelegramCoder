from aiogram import Router


router = Router()

# health (админ): показать uptime, статус планировщика, наличие активных опросов/ время до следующего.
# logs, alllogs (админ): показать последние ~100 строк лога / отправить весь лог.
